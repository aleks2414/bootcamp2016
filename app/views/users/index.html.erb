<p id="notice"><%= notice %></p>

<div class="container">
  <div class="col-sm-10 col-sm-offset-1">
    <div class="row">
    <div id="formariol">
      <ul>
        <% if current_user.admin? %>
          <% @users.each do |user| %>
          <li class="col-sm-2">
          <h2>Equipo: <%= user.team %></h2>
          <%= link_to 'Ver prueba', user %>
          </li>
          <% end %>
        <% else %>
          <% @usera.each do |user| %>
          <li class="col-sm-2">
          <h2>Equipo: <%= user.team %></h2>
          <%= link_to 'Ver prueba', user %>
          </li>
          <% end %>
        <% end %>
      </ul>
    </div>
    </div>
  </div>
</div>

<!-- <h1>Listing Users</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Admin</th>
      <th>team</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.admin %></td>
        <td><%= user.team %></td>
        <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Edit', edit_user_registration_path %></td>
        <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path %> -->
