<%= form_for([@user, @first_test]) do |f| %>
  <% if @first_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@first_test.errors.count, "error") %> prohibited this first_test from being saved:</h2>

      <ul>
      <% @first_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h2>Fase <strong>1. PROBLEMA</strong></h2>
  <hr class="blue">

  <p class="center">Adjunta el archivo con la infografía que utilizaste para hacer la primera validación
</p>

  <div class="field">
    <%= f.label :info1 %><br>
    <%= f.text_field :info1 %>
  </div>
  <br><br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
